services:
  web:
    image: ghcr.io/picosh/pico/git-web:latest
    restart: always
    volumes:
      - ./data/git-pr:/app/ssh_data
  ssh:
    image: ghcr.io/picosh/pico/git-ssh:latest
    restart: always
    volumes:
      - ./data/git-pr:/app/ssh_data
